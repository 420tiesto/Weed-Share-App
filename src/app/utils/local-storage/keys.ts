/**
 * Stores the access token and refresh token received on login to lens server.
 */
export const LENS_TOKENS = 'LENS_TOKENS';

/**
 * Stores the profile image URI of the user.
 */
export const USER_PROFILE_URI = 'USER_PROFILE_URI';

/**
 * Stores the follow image URI of the user
 */
export const USER_FOLLOW_URI = 'USER_FOLLOW_URI';